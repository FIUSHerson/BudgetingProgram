using System;
using System.IO;

namespace BudgetingProgram {
    public class Saving {
        public bool IsSaving() {
            /* This is a check to see if
            the user is currently saving anything. */
            return true;
        }
        public double TotalPrice() {
            /* Subtracts amount to save from TempBalance */
            return 0;
        }

    }
}